# M4（打磨收尾）验收记录 · 2026-09-01

自动化验证（29 测试全绿）：

| 项 | 方式 | 结果 |
|---|---|---|
| config.toml 解析（theme/workspaces/[keybinds]/none/[ghostty] 透传） | ConfigStoreTests | ✅ |
| KeyCombo 解析（cmd/super/shift/alt/ctrl+键） | ConfigStoreTests | ✅ |
| 改键生效/旧组合移除/解绑/工作区扩至 10（Cmd+6…0） | ConfigStoreTests | ✅ |
| 状态持久化 round-trip（树结构+叶数+activeIndex） | ConfigStoreTests | ✅ |
| Cmd+K = 速查（忠实 Omarchy 偏移 2） | KeybindingMapTests | ✅ |
| 生产路径状态保存/恢复 | 运行验证（quit→state.json 5 trees→重启恢复+shell 起） | ✅ |

会话中排掉的两个硬骨头（详见 porting-notes）：

1. **macOS 26 CVDisplayLink 会话配额**：数百次 surface 创建后 Create 失败（伪装成 OOM），
   连历史 tag 构建全灭。经 Debug 引擎 + @errorReturnTrace 定位，overlay 注入
   `window-vsync = false` 根治。若要 vsync，注销重登后在 config `[ghostty]` 覆盖。
2. **测试宿主生命周期绞杀**：TEST_HOST 下状态恢复/空树关窗/最后窗口退出与测试互相干扰，
   且 `override func setUp() async` 剥离 @MainActor 导致后台线程发布状态炸宿主。
   已做测试模式隔离 + 移除 async setUp。

待用户手验（醒来试一遍）：

- [ ] `Cmd+Alt+Space` 主菜单（图标条目、↑↓/回车/Esc/点击）；设置项打开 config.toml（自动写模板）
- [ ] `Cmd+K` 速查表（含 config 改键后的实时内容）
- [ ] `Cmd+S` Scratchpad：浮动终端、再按隐藏、跨工作区保持、点暗处关闭
- [ ] 编辑 `~/.config/quickterm/config.toml`（如 `workspaces = 7`、`new-terminal = "cmd+t"`、
      `[ghostty] font-size = 18`）保存即热生效
- [ ] 退出重开：布局/工作区/各 pane cwd 恢复
- [ ] `Ctrl+Cmd+F` 全屏（隐藏菜单栏/Dock）往返
- [ ] Dock 图标显示 QuickTerm 图标
