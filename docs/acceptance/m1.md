# M1 (tiling core) acceptance record · 2026-09-01

Automated verification (15 unit/integration tests green, `xcodebuild test`):

| Item | How | Result |
|---|---|---|
| §5.1 keybinding table fully bound (21 entries) | KeybindingMapTests.testSpec51TableFullyBound | ✅ |
| resize +Shift fine steps / terminal keys passed through (Cmd+C/V/-/K/Q) | KeybindingMapTests | ✅ |
| dwindle direction / swap / toggle-direction / out-of-range throws | SplitTreeQuickTermTests (5 cases) | ✅ |
| focus on hover enabled (focusFollowsMouse=true) | MainWindowControllerTests | ✅ |
| third layer of the config chain injected (unfocused-split-opacity=0.96 read back end to end) | MainWindowControllerTests | ✅ |
| pane insert/close, sibling reclaims the space | MainWindowControllerTests | ✅ |
| hidden title bar + NSHostingView + SurfaceScrollView chain | EngineSmokeTests | ✅ |

Implemented in code, still to be checked by hand (one pass when you wake up):

- [ ] Cmd+Return three or more panes in a row: dwindle splits right/down, pop-in animation, gaps 5/10, accent border on the focused pane
- [ ] Hover activates (outer border highlights, you can type straight away); unfocused panes dim (0.96)
- [ ] Cmd+arrow / Cmd+Shift+arrow / Cmd+J / Cmd+F (zoom) / Cmd+Ctrl+arrow (±Shift) / Cmd+Ctrl+= / Alt+Tab / Cmd+[ ]
- [ ] Cmd+W closes a pane (confirmation dialog when a process is running); closing the last one closes the window
- [ ] ⌘ + left-drag a pane: centre of the target = swap, edge = split and insert (blue highlight marks the zone)
- [ ] ⌘ + right-drag: smooth resize
- [ ] Drag a divider to change the ratio, double-click a divider to equalise
