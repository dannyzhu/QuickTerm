# M3（主题 + 背景）验收记录 · 2026-09-01

自动化验证（24 测试全绿）：

| 项 | 方式 | 结果 |
|---|---|---|
| colors.toml 解析（引号/行尾注释/mode） | ThemeTests | ✅ |
| 内置 22 主题发现（tokyo-night 默认、含 5 个 light） | ThemeTests | ✅ |
| overlay 配色映射 = omarchy ghostty.conf.tpl（palette 0–15/bg/fg/cursor/selection） | ThemeTests | ✅ |
| 透明度 toggle 覆盖基础段（同键后写者胜） | ThemeTests | ✅ |
| overlay 落盘并被引擎首次加载读取 | 运行时验证（engine-overlay.conf 含主题色） | ✅ |

实现说明：
- 主题资产：22 个 Omarchy 官方主题（master 快照，比设计时的 19 个多 last-horizon/lupine/solitude）；colors.toml 进 git，每主题 1 张官方背景由 `scripts/fetch-themes.sh` 拉取（不进 git）
- 热切换路径：ThemeManager 重写 overlay → `ghostty.reloadConfig` app 级 + 全工作区逐 surface → UI 调色板（顶栏/边框/面板）同步；浅色主题联动窗口 appearance + `ghostty_app_set_color_scheme`
- 主题/背景选择器：Walker 风格居中面板（Monaco、2px accent 边框、直角、0.95 透明），↑↓/回车/Esc 键盘导航 + 点击

待用户手验：

- [ ] `Cmd+Ctrl+Shift+Space` 主题选择器：色板行、light 标记、回车应用——终端色+顶栏+边框+壁纸整体热切换（无重启）
- [ ] `Cmd+Ctrl+Space` 背景选择器；面板开着再按一次 = 循环下一张（回绕）
- [ ] 壁纸透过 gaps 与微透明 pane 可见；空工作区全幅壁纸
- [ ] `Cmd+Backspace` 透明度开关；`Cmd+Shift+Backspace` gaps 开关
- [ ] 选浅色主题（如 Catppuccin Latte）窗口外观联动
- [ ] 重启 app 记住上次主题与背景
