# M2 (workspaces + status bar) acceptance record · 2026-09-01

Automated verification (20 tests green):

| Item | How | Result |
|---|---|---|
| 5 workspaces by default / switching is independent / out-of-range is safe | WorkspaceTests | ✅ |
| move a pane to an empty workspace and follow it, source tree reclaims the space | WorkspaceTests | ✅ |
| Cmd+1…5 / Cmd+Shift+1…5 / Cmd+Shift+Space bindings; Cmd+6 passed through | WorkspaceTests | ✅ |
| the window only closes once every workspace is empty (multi-workspace close semantics) | code path + removePane fix | ✅ |

To be checked by hand (one pass when you wake up):

- [ ] Status bar visuals: 26pt, left `◆` + capsules (active `■` in the accent colour, empty at 50% opacity), middle `Sunday 14:32`, right cpu% / wifi / volume / battery
- [ ] Click a capsule to switch; scroll the status bar to cycle workspaces; click the clock for the full date; click the volume icon to mute
- [ ] `Cmd+1…5` switches instantly (no animation); `Cmd+Shift+<digit>` moves a pane and follows it
- [ ] `Cmd+Shift+Space` hides/shows the status bar, the content area grows and shrinks with it
- [ ] An empty workspace shows the background colour, `Cmd+Return` opens the first pane in it
