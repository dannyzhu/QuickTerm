# M5 (scrolling infinite canvas) acceptance record · 2026-09-01

Automated verification (40 tests green, 11 of them new):

| Item | How | Result |
|---|---|---|
| insert a column on the right / delete an empty column / focus in four directions (no wrapping, nearest row when crossing columns) | ScrollingStripTests | ✅ |
| reorder columns + reorder within a column / Cmd+J stack into a column ⇄ split back out | ScrollingStripTests | ✅ |
| column width ±5%, clamped to 25–90% / equalise resets to 0.49 | ScrollingStripTests | ✅ |
| **minimal scrolling to follow the viewport** (including exact assertions for both "peek" cases) | pure-function test on targetOffset | ✅ |
| five drop zones (left/right insert a column, top/bottom stack, centre swaps) | ScrollingStripTests | ✅ |
| dwindle ⇄ scrolling conversion keeps every pane / Codable round-trip | ScrollingStripTests | ✅ |
| new workspaces default to scrolling / Cmd+L round-trip keeps panes / Cmd+L binding | WorkspaceTests | ✅ |
| moving across workspaces (the target inserts according to its own layout) | WorkspaceTests | ✅ |
| persistence v2 (layout type + pane count round-trip) | ConfigStoreTests | ✅ |
| all existing dwindle behaviour | the original 29 tests stay green | ✅ |
| runtime: fresh launch in scrolling, shells come up, zero surface errors | runtime check | ✅ |

To be checked by hand (against your Omarchy screenshots):

- [ ] `Cmd+Return` three times: the third column **peeks in at the right edge** (behaviour in screenshot 1)
- [ ] `Cmd+→` focus to the third column: the viewport scrolls, **the column on the left peeks in at the left edge** (behaviour in screenshot 2); ~0.15s, smooth
- [ ] A new pane always lands to the right of the current column (behaviour in screenshot 3)
- [ ] `Cmd+J` stacks into the column on the left / press again to split back out; `Cmd+↑/↓` moves focus within a column
- [ ] `Cmd+Shift+←/→` reorders whole columns; `Cmd+Ctrl+←/→` changes column width; `Cmd+Ctrl+=` resets
- [ ] ⌘ + left-drag onto another column's edge/centre; ⌘ + right-drag to change column width
- [ ] Two-finger horizontal trackpad swipe pans the canvas and snaps on release (bonus item)
- [ ] `Cmd+L` back to dwindle (old behaviour intact) and back again
- [ ] Quit and reopen restores the scrolling layout and the column structure

## v6 addendum (same day)

- Pane padding defaults to 3pt and is configurable (`pane-padding`), with 4 tests covering the clamp, overlay injection and the theme=ghostty mode ✅ (the registry default is 14 today — Omarchy's own value — and the spacing around a pane is `pane-gap`, default 5)
- One column fills the viewport / two columns sit centred with equal gaps either side / three columns overflow and go back to hugging the left edge — 3 geometry tests ✅ (45 tests green)
- By hand: one column should fill the screen; with a second, the gaps either side are equal; the third makes a column peek in at the right edge (same as the screenshots)
