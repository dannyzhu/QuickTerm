# M5（scrolling 无限画布）验收记录 · 2026-09-01

自动化验证（40 测试全绿，新增 11 个）：

| 项 | 方式 | 结果 |
|---|---|---|
| 右插新列 / 空列删除 / 焦点四向（不回绕、跨列就近行） | ScrollingStripTests | ✅ |
| 列换位 + 列内换位 / Cmd+J 併栈⇄拆列 | ScrollingStripTests | ✅ |
| 列宽 ±5% clamp 25–90% / 等分重置 0.49 | ScrollingStripTests | ✅ |
| **最小滚动视口跟随**（含"露边"两个场景的精确断言） | targetOffset 纯函数测试 | ✅ |
| 拖放五区（左右插列/上下併栈/中心交换） | ScrollingStripTests | ✅ |
| dwindle⇄scrolling 互转保 pane / Codable 往返 | ScrollingStripTests | ✅ |
| 新工作区默认 scrolling / Cmd+L 往返保 pane / Cmd+L 绑定 | WorkspaceTests | ✅ |
| 跨工作区移动（目标按其布局插入） | WorkspaceTests | ✅ |
| 持久化 v2（布局类型 + pane 数往返） | ConfigStoreTests | ✅ |
| dwindle 全部既有行为 | 原 29 测试保持全绿 | ✅ |
| 运行时：fresh 启动 scrolling、shell 拉起、零 surface 错误 | 运行验证 | ✅ |

待用户手验（对照你的 Omarchy 截图）：

- [ ] `Cmd+Return` 连开 3 个：第三列在**右缘露出一条**（截图 1 行为）
- [ ] `Cmd+→` 焦点到第三列：视口滚动，**左侧列在左缘露出**（截图 2 行为）；~0.15s 平滑
- [ ] 新 pane 永远插在当前列右侧（截图 3 行为）
- [ ] `Cmd+J` 併入左列纵栈 / 再按拆出；`Cmd+↑/↓` 列内焦点
- [ ] `Cmd+Shift+←/→` 整列换位；`Cmd+Ctrl+←/→` 调列宽；`Cmd+Ctrl+=` 重置
- [ ] ⌘+左键拖到别列边缘/中心；⌘+右键拖调列宽
- [ ] 触控板双指横滑平移画布、松手吸附（附带项）
- [ ] `Cmd+L` 切回 dwindle（原行为完好）再切回
- [ ] 退出重开恢复 scrolling 布局与列结构
